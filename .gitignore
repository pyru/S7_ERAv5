# python
__pycache__/
*.pyc

# local tooling config
.claude/

# regenerable from the corpus + tokenizer: `python -c "from spectral.data import
# build_stream; build_stream(32000)"`.  The corpus itself IS committed, because
# it came from a random-article sample and is therefore not reproducible.
data/*.npy
data/*.log

# screenshots and scratch
shot_*.png
slice*.png
